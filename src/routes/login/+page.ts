import { redirect } from "@sveltejs/kit";

export async function load({ url }) {
  // This page load function can be used for additional checks
  // The main redirect logic is handled in the component itself
  return {};
}
